export const selectAdverts = (state) => state.adverts.list;

export const selectFavorites = (state) => state.adverts.favorites;

export const selectMakeFilter = (state) => state.adverts.makeFilter;

export const selectPage = (state) => state.adverts.page;

export const selectPriceFilter = (state) => state.adverts.priceFilter;

export const selectMileageFilter = (state) => state.adverts.mileageFilter;

export const selectHasMore = (state) => state.adverts.hasMore;
